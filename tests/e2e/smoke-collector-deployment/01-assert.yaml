apiVersion: apps/v1
kind: Deployment
metadata:
  name: smoke-collector-collector
spec:
  template:
    spec:
      (contains(containers[?name == 'otc-container'].args[], '--extra-arg=no')): true
status:
  readyReplicas: 1
